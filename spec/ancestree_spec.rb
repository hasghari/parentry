require 'spec_helper'

describe Ancestree do
  it 'has a version number' do
    expect(Ancestree::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
